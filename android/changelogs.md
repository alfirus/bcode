# Changelog

## 0.1.0

- Own minimalist futuristic chat UI: header status, Chat / Sessions / Settings tabs
- Session list, new session, blocking send with working indicator, live SSE text tail
- Permission Approve / Deny cards (default-deny)
- Server URL + password in plugin-scoped secret storage
- `bcode: open remote chat` command
